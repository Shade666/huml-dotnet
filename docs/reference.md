# Reference

Look-up material — terse, complete, and organised for scanning rather than reading end to end.

- **API reference** — every public type and member, generated from the source XML docs.
  Start at <xref:Huml.Net.HumlSerializer>, the single public entry point.
- [Options reference](options-reference.md) — every `HumlOptions` property, its default, and
  the preset instances (`Default`, `LatestSupported`, `Strict`).
- [Attributes reference](attributes-reference.md) — every `[Huml*]` attribute with its targets
  and precedence rules.

Looking for a task-oriented walkthrough instead? See the [how-to guides](how-to-guides.md).
