# Huml.Net Backlog

## Purpose

This file provides public visibility into planned work for Huml.Net. It is the canonical list of
accepted items that are tracked for implementation across the project.

## How It Works

- Users report bugs and request features via GitHub Issues.
- The maintainer triages issues and promotes accepted items to this backlog.
- Internal planning workflows are not publicly exposed — this file provides transparency into
  what is planned, in progress, and done.
- Items move through statuses: Planned -> In Progress -> Done.

## Backlog

| Category      | Item                                                                                                                                                                                                                                                     | Version | Priority | Status  |
| ------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | -------- | ------- |
| API           | Implement type-directed dispatch in `Serialize(object?, Type)` — currently ignores the `Type` parameter (section 2.3, task 1.1)                                                                                                                          | V1      | High     | Done    |
| Documentation | Add `<remarks>` to `Huml.Deserialize<T>(ReadOnlySpan<char>)` documenting span-to-string allocation (section 2.3, task 1.2)                                                                                                                               | V1      | Medium   | Done    |
| Documentation | Add XML doc to `HumlDocument` clarifying dual role as document root and nested mapping block (section 2.3, task 1.3)                                                                                                                                     | V1      | Low      | Done    |
| Performance   | Add property-lookup dictionary to `PropertyDescriptor` cache for O(1) deserialiser key lookup (section 5.1, task 2.1)                                                                                                                                    | V1      | Low      | Done    |
| Performance   | Cache indent strings in `HumlSerializer.Indent()` to eliminate per-call allocation (section 5.1, task 2.2)                                                                                                                                               | V1      | Low      | Done    |
| Performance   | Pool `StringBuilder` in serialiser via `[ThreadStatic]` to reduce GC pressure (section 5.1, task 2.3)                                                                                                                                                    | V2      | Medium   | Done    |
| Performance   | Refactor Lexer to `ref struct` accepting `ReadOnlySpan<char>` for genuine zero-copy deserialisation (section 9, phase 3)                                                                                                                                 | V2      | High     | Done    |
| Diagnostics   | Carry source position (Line, Column) through AST nodes for richer `HumlDeserializeException` context (section 9, task 4.1)                                                                                                                               | V2      | Medium   | Done    |
| API           | Add `HumlOptions` factory method for "header-detected, latest fallback" variant (section 9, task 4.2)                                                                                                                                                    | V2      | Low      | Done    |
| Testing       | Add concurrency test for `PropertyDescriptor` cache under parallel deserialisation (section 9, task 4.3)                                                                                                                                                 | V2      | Low      | Planned |
| Documentation | Add CHANGELOG.md with version history from git tags (section 8.2)                                                                                                                                                                                        | V1      | Low      | Done    |
| Security      | Document uncapped document size limitation; consider optional `MaxDocumentSize` option (section 6.1)                                                                                                                                                     | V2      | Low      | Planned |
| API           | Add `HumlOptions.PropertyNamingPolicy` and built-in `HumlNamingPolicy.KebabCase`/`SnakeCase`/`CamelCase`/`PascalCase` policies for automatic key↔property name mapping (Phase 999.18)                                                                    | V2      | High     | Done    |
| API           | Add enum deserialisation support — `Enum.TryParse` with case-insensitive fallback plus optional `[HumlEnumValue]` per-member name override (Phase 999.19)                                                                                                | V2      | High     | Done    |
| API           | Add enum serialisation support — emit member name (or `[HumlEnumValue]`) honouring naming policy; enables round-trip for enum-valued properties (Phase 999.20)                                                                                           | V2      | High     | Done    |
| API           | Add custom converter API: `HumlConverter<T>` abstract base, `[HumlConverter]` attribute, `HumlOptions.Converters` collection (Phase 999.21)                                                                                                              | V2      | High     | Done    |
| API           | Add `Huml.Populate<T>(string, T existing, options?)` for config-overlay deserialisation into an existing object instance (Phase 999.22)                                                                                                                  | V2      | Medium   | Done    |
| API           | Add `HumlOptions.DefaultIgnoreCondition` / `[HumlIgnoreDefaults]` for type-level default-omit without per-property `OmitIfDefault` (Phase 999.23)                                                                                                        | V2      | Low      | Done    |
| API           | Add `HumlOptions.ValidateDuplicateKeysOnWrite` to catch dictionary key collisions during serialisation (Phase 999.24)                                                                                                                                    | V2      | Low      | Done    |
| API           | Add `IReadOnlySet<T>` deserialisation support to collection dispatch (Phase 999.25)                                                                                                                                                                      | V2      | Low      | Done    |
| API           | Constructor parameter binding and `init`-only setter support — allow `HumlDeserializer` to bind records, parameterised ctors, and `init`-only properties (Phase 999.26)                                                                                  | V2      | High     | Done    |
| API           | Required-property enforcement via `[HumlRequired]` attribute and C# `required` modifier; throws on missing keys (Phase 999.27)                                                                                                                           | V2      | High     | Done    |
| API           | Extension data via `[HumlExtensionData]` — capture unknown keys into a `Dictionary<string, HumlNode>` property for forward-compatible config consumption (Phase 999.28)                                                                                  | V2      | Medium   | Done    |
| API           | Polymorphic (de)serialisation with `[HumlDerivedType]` discriminator — dispatch base-class/interface properties via configurable `$type` key (Phase 999.29)                                                                                              | V3      | Medium   | Planned |
| Tooling       | AOT / trim safety annotations — add `[RequiresUnreferencedCode]`, `[RequiresDynamicCode]`, `[DynamicallyAccessedMembers]` to reflection-using public API; add `<IsTrimmable>true</IsTrimmable>` (Phase 999.30)                                           | V2      | High     | Done    |
| API           | Source-generator seam — define `IHumlTypeInfoResolver` / `HumlTypeInfo<T>` and `HumlOptions.TypeInfoResolver` for future AOT plug-in without breaking changes (Phase 999.31)                                                                             | V2      | Medium   | Done    |
| Performance   | Source-generator implementation — Roslyn incremental generator emitting compiled `HumlTypeInfo<T>`, shipped as `Huml.Net.SourceGeneration` NuGet package; requires Phase 999.31 (Phase 999.32)                                                           | V3      | Medium   | Planned |
| API           | Number handling modes — `HumlOptions.NumberHandling` flags enum supporting `AllowReadingFromString`, `WriteAsString`, `AllowNamedFloatingPointLiterals` (Phase 999.33)                                                                                   | V3      | Low      | Planned |
| API           | Missing-member handling — `HumlOptions.UnmappedMemberHandling` (`Skip` default, `Disallow`) for strict rejection of unknown HUML keys (Phase 999.34)                                                                                                     | V3      | Low      | Planned |
| API           | Per-member naming-policy override attribute — `[HumlNamingPolicy(typeof(…))]` for member-level convention override independent of `HumlOptions.PropertyNamingPolicy` (Phase 999.35)                                                                      | V3      | Low      | Planned |
| API           | `HumlOptions.Strict` preset — bundles all strict/validation toggles into one factory; requires Phase 999.24/27/34 (Phase 999.36)                                                                                                                         | V3      | Low      | Planned |
| API           | Native `DateOnly` / `TimeOnly` round-trip — built-in ISO-8601 handling matching STJ .NET 10 defaults; confirm `DateTime`/`DateTimeOffset`/`TimeSpan` coverage (Phase 999.37)                                                                             | V2      | Medium   | Done    |
| API           | Add `SortedSet<T>` deserialisation support — materialise as `SortedSet<T>` with default comparer; separate branch from the `HashSet<T>` path added in Phase 16; no conditional compile required (`SortedSet<T>` exists on netstandard2.1) (Phase 999.38) | V2      | Low      | Planned |
| API           | Full STJ parity for `HumlTypeInfo<T>` — `Properties` collection, `HumlPropertyInfo<T>` per-property metadata, `CreateObject` delegate, on-serialising/on-deserialised callbacks; mirrors `JsonTypeInfo<T>`; requires Phase 999.32 (Phase 999.39)         | V3      | Medium   | Planned |
| Performance   | Cache `IEnumerable<T>` interface lookup in `DeserializeSequence` — uncached `GetInterface()` call per collection deserialise; add `ConcurrentDictionary<Type, Type?>` static cache (Phase 999.40)                                                        | V3      | Low      | Planned |
| API           | Add `HumlOptions.MakeReadOnly()` to freeze options after first use — throws `InvalidOperationException` on post-freeze mutation; pre-built instances pre-frozen; mirrors STJ .NET 7+ (Phase 999.41)                                                      | V3      | Low      | Planned |
| Performance   | Replace `Convert.ChangeType` with explicit `checked` casts in `CoerceScalar` for common integral target types (`int`, `long`, `short`, `byte`, `uint`, `ulong`); retain fallback for exotic numerics (Phase 999.42)                                      | V3      | Low      | Planned |
| Diagnostics   | Add property name and containing type name to `HumlSerializeException` messages for easier diagnosis of unsupported-type serialisation errors (Phase 999.43)                                                                                             | V3      | Low      | Planned |
| Testing       | Add fuzz/property-based test suite — 20+ randomised inputs assert parser never throws `StackOverflowException` or `NullReferenceException`; only `HumlParseException` or no exception (Phase 999.44)                                                     | V3      | Low      | Planned |
| API           | Add `IBufferWriter<char>` output overload to `HumlSerializer` — allocation-free serialisation for high-throughput ASP.NET/`System.IO.Pipelines` scenarios (Phase 999.45)                                                                                 | V4      | Low      | Planned |
| Bug           | CR-01: `ScanBacktickMultiline` (v0.1) captures indented content with leading spaces intact — no strip-indent applied unlike the v0.2 triple-quote scanner; silent data corruption when parsing v0.1 documents via `VersionSource.Header` (Phase 999.46, see [CODEBASE-REVIEW.md](.planning/CODEBASE-REVIEW.md)) | V2      | High     | Planned |
| Bug           | CR-03: `ParseInt` throws raw `OverflowException` for large hex literals (e.g. `0x8000000000000000`) instead of `HumlParseException` — breaks the documented exception contract of `Huml.Parse()` (Phase 999.47, see [CODEBASE-REVIEW.md](.planning/CODEBASE-REVIEW.md)) | V2      | High     | Planned |
| Bug           | CR-04: `TimeOnly` round-trip asymmetry — serialiser emits `HH:mm:ss.FFFFFFF` but deserialiser uses `TimeOnly.Parse` (multi-format); switch deserialiser to `ParseExact` to mirror the emitter (Phase 999.48, see [CODEBASE-REVIEW.md](.planning/CODEBASE-REVIEW.md)) | V2      | Medium   | Planned |
| Bug           | CR-05: Converter re-entry guard in `EmitEntry` is bypassed when `value` is `null` — potential infinite recursion for a converter that calls `AppendSerializedValue(null)` for the same type it handles (Phase 999.49, see [CODEBASE-REVIEW.md](.planning/CODEBASE-REVIEW.md)) | V2      | High     | Planned |
| Bug           | CR-06: `alreadyBound` uses `OrdinalIgnoreCase` but `lookup` uses `Ordinal` in `DeserializeMappingEntries` — required-property check falsely satisfied for case-variant HUML keys when a `[HumlConstructor]`-selected ctor is used (Phase 999.50, see [CODEBASE-REVIEW.md](.planning/CODEBASE-REVIEW.md)) | V2      | High     | Planned |
| Bug           | WR-02: `ScanDoubleQuoteToken` key-detection heuristic (column + one-char `:` lookahead) can misclassify a quoted value as a key in inline dicts; pass `isValueContext` flag from callers to disambiguate (Phase 999.51, see [CODEBASE-REVIEW.md](.planning/CODEBASE-REVIEW.md)) | V2      | Medium   | Planned |
| Bug           | WR-06: `IsStringKeyedDictionary` only matches `Dictionary<K,V>` — `IDictionary<string,T>` falls through to POCO deserialization and throws a misleading "no parameterless constructor" error; extend the check to include `IDictionary<,>` (Phase 999.52, see [CODEBASE-REVIEW.md](.planning/CODEBASE-REVIEW.md)) | V2      | Medium   | Planned |
| Bug           | WR-08: `ParseFloat` has no `try/catch` for `FormatException` — a malformed float token propagates as an unhandled framework exception rather than `HumlParseException` (Phase 999.53, see [CODEBASE-REVIEW.md](.planning/CODEBASE-REVIEW.md)) | V2      | Low      | Planned |
| Bug           | WR-11: `DeserializeDictionary` does not handle `IDictionary<string,T>` — the type is silently misrouted and throws a misleading error; materialise as `Dictionary<string,T>` when the target interface is `IDictionary<,>` (Phase 999.54, see [CODEBASE-REVIEW.md](.planning/CODEBASE-REVIEW.md)) | V2      | Medium   | Planned |
| Testing       | WR-12: `SharedSuiteTests.LoadFixtures` calls `Directory.GetFiles` without an existence check — uninitialised git submodules produce a confusing `DirectoryNotFoundException`; add a guard with a helpful "run git submodule update --init" message (Phase 999.55, see [CODEBASE-REVIEW.md](.planning/CODEBASE-REVIEW.md)) | V2      | Low      | Planned |
| Bug           | IN-04: `HumlConverter<T>.WriteObject` calls `Write(context, default!)` for null reference-type values — if the converter does not handle null it will throw `NullReferenceException`; emit `null` directly for reference types (Phase 999.56, see [CODEBASE-REVIEW.md](.planning/CODEBASE-REVIEW.md)) | V2      | Low      | Planned |
| Quality       | CR-02: `MeasureIndent` contains the no-op tautology `_pos = p - indent + indent` with a misleading comment; replace with `_pos = p` and an accurate comment to prevent a future "fix" from breaking position tracking (Phase 999.57, see [CODEBASE-REVIEW.md](.planning/CODEBASE-REVIEW.md)) | V2      | Low      | Planned |
| Quality       | WR-01: `Huml.Populate<T>(string, T, options)` does not guard `existing` for null at the string-overload level — null check lives only in the callee; add an explicit guard consistent with the `huml` null guard already present (Phase 999.58, see [CODEBASE-REVIEW.md](.planning/CODEBASE-REVIEW.md)) | V2      | Low      | Planned |
| Quality       | WR-03: `ScanComment` uses two separate `if` checks for EOF-after-hash and non-space-after-hash — simplify to a single condition for clarity and correctness (Phase 999.59, see [CODEBASE-REVIEW.md](.planning/CODEBASE-REVIEW.md)) | V3      | Low      | Planned |
| Performance   | WR-04: `PropertyDescriptor.BuildDescriptors` calls `Activator.CreateInstance` for `Nullable<T>` properties at cache-build time — the default is always `null` for nullable types; skip the allocation and assign `null` directly (Phase 999.60, see [CODEBASE-REVIEW.md](.planning/CODEBASE-REVIEW.md)) | V3      | Low      | Planned |
| Quality       | WR-05: `HumlOptions.Converters` is accepted as `IReadOnlyList<T>` which can be mutated via cast after first resolution; copy to an immutable array during options init to prevent stale `ConverterResolutionCache` entries (Phase 999.61, see [CODEBASE-REVIEW.md](.planning/CODEBASE-REVIEW.md)) | V3      | Low      | Planned |
| Documentation | WR-07: `HumlConverter<T>.Write` and `HumlSerializerContext.AppendSerializedValue` lack warnings that property-level converters must emit single-line scalar output only — multi-line output silently produces invalid HUML (Phase 999.62, see [CODEBASE-REVIEW.md](.planning/CODEBASE-REVIEW.md)) | V2      | Low      | Planned |
| Quality       | WR-09: `HumlOptions.Default` and `HumlOptions.LatestSupported` share mutable `ConverterResolutionCache`; test teardown clears the global `InstanceCache` but not the per-options resolution cache — add `ClearOptionsCaches()` internal helper called from test setup (Phase 999.63, see [CODEBASE-REVIEW.md](.planning/CODEBASE-REVIEW.md)) | V2      | Low      | Planned |
| Quality       | WR-10: `InferScalarOrInlineListRootType` comment says "Prepend both back" but only `firstToken` is stored in `_pending` — `nextToken` was never consumed from `_lookahead`; correct the comment to prevent future maintainer confusion (Phase 999.64, see [CODEBASE-REVIEW.md](.planning/CODEBASE-REVIEW.md)) | V3      | Low      | Planned |
| Documentation | IN-01: `HumlIgnoreCondition.Always` XML doc claims it is "equivalent to WhenWritingNull \| WhenWritingDefault" but the serialiser treats `Always` as unconditional omission even for non-default, non-null values — clarify the distinction in the XML doc (Phase 999.65, see [CODEBASE-REVIEW.md](.planning/CODEBASE-REVIEW.md)) | V2      | Low      | Planned |
| Testing       | IN-02: Tests using `HumlOptions.Default`/`LatestSupported` share `ConverterResolutionCache` across the test run — test constructors should also clear per-options resolution caches (not just the global `InstanceCache`) to prevent cross-test pollution (Phase 999.66, see [CODEBASE-REVIEW.md](.planning/CODEBASE-REVIEW.md)) | V2      | Low      | Planned |
| Documentation | IN-03: `HumlNamingPolicy.Separate` XML doc example `URL → u-r-l` does not document digit behaviour (e.g. `B2B → b-2-b`); document the digit-as-word-boundary convention explicitly (Phase 999.67, see [CODEBASE-REVIEW.md](.planning/CODEBASE-REVIEW.md)) | V3      | Low      | Planned |
| Quality       | IN-05: `HumlDeserializeException` three-argument constructor `(message, key, line)` omits column from the diagnostic message — mark obsolete or internal in favour of the four-argument constructor that includes column position (Phase 999.68, see [CODEBASE-REVIEW.md](.planning/CODEBASE-REVIEW.md)) | V3      | Low      | Planned |
